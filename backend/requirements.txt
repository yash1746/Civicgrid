# ──────────────────────────────────────────────
# CivicGrid Backend — Python Dependencies
# ──────────────────────────────────────────────

# Web Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Supabase (DB + Auth + Storage — PostGIS enabled)
supabase==2.10.0
gotrue==2.9.0
postgrest==0.18.0

# Google Gemini AI (Vision Agent + Orchestration)
google-generativeai==0.8.3

# Scheduling (Escalation Agent SLA timer)
apscheduler==3.10.4

# Auth / Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1
pydantic-settings==2.6.1

# Social Media APIs
tweepy==4.14.0
instagrapi==2.1.2

# Email
sendgrid==6.11.0

# Image / EXIF processing (server-side validation)
pillow==11.0.0
piexif==1.1.3

# HTTP Client
httpx==0.28.1
aiofiles==24.1.0

# Data validation & serialization
pydantic[email]==2.10.3

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
